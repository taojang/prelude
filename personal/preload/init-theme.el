(setq prelude-theme 'solarized-dark)

(setq initial-frame-alist '(
                            (font . "Noto Mono-12")
                            ))
(setq default-frame-alist '(
                            (font . "Noto Mono-12")
                            ))
