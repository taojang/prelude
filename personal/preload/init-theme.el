(setq prelude-theme 'solarized-light)

(setq initial-frame-alist '(
                            (font . "Noto Mono-13")
                            ))
(setq default-frame-alist '(
                            (font . "Noto Mono-13")
                            ))
